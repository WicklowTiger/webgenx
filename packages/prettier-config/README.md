# Prettier configuration

# Usage:

**Install**:

```bash
$ npm install --save-dev @webgenx/prettier-config
```

**Edit `package.json`**:

```jsonc
{
	// ...
	"prettier": "@webgenx/prettier-config"
}
```
